package com.just.youtube

import androidx.car.app.testing.CarAppServiceController
import androidx.car.app.testing.TestCarContext
import androidx.car.app.model.PaneTemplate
import androidx.car.app.model.Row
import org.junit.Assert.assertEquals
import org.junit.Assert.assertNotNull
import org.junit.Test
import org.junit.runner.RunWith
import org.robolectric:RobolectricTestRunner

@RunWith(RobolectricTestRunner::class)
class MyCarScreenTest {

    @Test
    fun testScreenTemplateContents() {
        // 1. ?????? ???? ??????? ???? ??????? ????? ??? VS Code
        val testCarContext = TestCarContext.createCarContext()
        val screen = MyCarScreen(testCarContext)

        // 2. ??????? ??????? ???? ???? ????????
        val template = screen.onGetTemplate() as PaneTemplate

        // 3. ??? ??????? ??????? ???????
        assertEquals("????????", template.title.toString())

        // 4. ??? ??????? ?????? ???? ??????? ?????? ?? ??????
        val pane = template.pane
        val rows = pane.rows
        assertEquals(1, rows.size)

        val firstRow = rows[0] as Row
        assertEquals("Car YouTube", firstRow.title.toString())
        assertEquals("?????? ?? ?? ????? ?????? ???????!", firstRow.texts[0].toString())
        
        println("==================================================")
        println("? ??? ??????? ??? ????? 100%!")
        println("???????: \")
        println("???? ????? ???????: \")
        println("==================================================")
    }
}
